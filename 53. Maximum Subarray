function maxSubArray(nums: number[]): number {
     let m = nums[0];
    let c = 0;
    for(let num of nums){
        if( c < 0 ){
            c = 0;
        };
        c += num;
        if( c > m ){
            m = c;
        };
    };
    return m;
};
