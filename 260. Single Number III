function singleNumber(nums: number[]): number[] {
     let demo:{[key:number]:number}={};
    for(let num of nums){
        if(!(num in demo)){
            demo[num]=1;
        }else{
            delete demo[num];
        }
    }
    return Object.keys(demo).map(Number)
    
};
