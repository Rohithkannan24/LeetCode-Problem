function moveZeroes(nums: number[]): void {
    let res=0;
    for(let i=0;i<nums.length;i++){
        if(nums[i]!==0){
            let temp=nums[i];
            nums[i]=nums[res];
            nums[res]=temp;
            res++;
        }
    }
    
};
